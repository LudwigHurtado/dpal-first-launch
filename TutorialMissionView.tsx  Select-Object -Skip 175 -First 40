[1mdiff --git a/components/TutorialMissionView.tsx b/components/TutorialMissionView.tsx[m
[1mindex de43ad3..83849ec 100644[m
[1m--- a/components/TutorialMissionView.tsx[m
[1m+++ b/components/TutorialMissionView.tsx[m
[36m@@ -1,8 +1,7 @@[m
[31m-[m
 import React, { useState, useMemo, useEffect } from 'react';[m
[31m-import { Category, type Report, type FieldPrompt, type MissionAction, type TutorialMission, type SeverityLevel } from '../types';[m
[32m+[m[32mimport { Category, type Report, type FieldPrompt, type SeverityLevel } from '../types';[m
 import { useTranslations } from '../i18n';[m
[31m-import { ArrowLeft, CheckCircle, Zap, Clock, ShieldCheck, Target, Camera, Loader, Database, Sparkles, Box, FileText, Activity, X } from './icons';[m
[32m+[m[32mimport { CheckCircle, Zap, Target, Camera, Loader, Sparkles, Activity, ShieldCheck } from './icons';[m
 import PanelShell from './Workspace/PanelShell';[m
 import { tutorialReportForDutyMission } from '../data/tutorialMission';[m
 import { tutorialOverlaySteps } from '../data/tutorialOverlaySteps';[m
[36m@@ -15,9 +14,13 @@[m [minterface TutorialMissionViewProps {[m
 [m
 const TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, onSkip }) => {[m
   const { t } = useTranslations();[m
[32m+[m
   const [activeActionIndex, setActiveActionIndex] = useState(0);[m
   const [isProcessing, setIsProcessing] = useState(false);[m
[31m-  [m
[32m+[m
[32m+[m[32m  // HARD GATE. DEFAULT OFF.[m
[32m+[m[32m  const [showTutorial, setShowTutorial] = useState(false);[m
[32m+[m
   // Data stores[m
   const [tutorialData, setTutorialData] = useState({ mode: '' as 'PRACTICE' | 'REAL' | '', operatingArea: '', reviewAccepted: false });[m
   const [draftReport, setDraftReport] = useState({ category: '' as Category | '', severity: 'Standard' as SeverityLevel, summary: '' });[m
[36m@@ -30,7 +33,7 @@[m [mconst TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, o[m
   // SAFETY: Allow ESC key to exit tutorial at any time[m
   useEffect(() => {[m
     const handleKeyDown = (e: KeyboardEvent) => {[m
[31m-        if (e.key === 'Escape') onSkip();[m
[32m+[m[32m      if (e.key === 'Escape') onSkip();[m
     };[m
     window.addEventListener('keydown', handleKeyDown);[m
     return () => window.removeEventListener('keydown', handleKeyDown);[m
[36m@@ -48,12 +51,12 @@[m [mconst TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, o[m
 [m
       if (prompt.responseType === 'checkbox') return val === true;[m
       if (prompt.responseType === 'text') {[m
[31m-          const rule = prompt.validationRules?.find(r => r.rule === 'minLength');[m
[31m-          const minLen = typeof rule?.value === 'number' ? rule.value : 1;[m
[31m-          return (val || '').length >= minLen;[m
[32m+[m[32m        const rule = prompt.validationRules?.find(r => r.rule === 'minLength');[m
[32m+[m[32m        const minLen = typeof rule?.value === 'number' ? rule.value : 1;[m
[32m+[m[32m        return (val || '').length >= minLen;[m
       }[m
       if (prompt.responseType === 'multi-select' || prompt.responseType === 'photo') {[m
[31m-          return val !== '' && val !== null && (Array.isArray(val) ? val.length > 0 : true);[m
[32m+[m[32m        return val !== '' && val !== null && (Array.isArray(val) ? val.length > 0 : true);[m
       }[m
       return !!val;[m
     });[m
[36m@@ -69,31 +72,31 @@[m [mconst TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, o[m
 [m
   const handleNext = () => {[m
     if (activeActionIndex < tutorialReportForDutyMission.actions.length - 1) {[m
[31m-        setIsProcessing(true);[m
[31m-        setTimeout(() => {[m
[31m-            setActiveActionIndex(prev => prev + 1);[m
[31m-            setIsProcessing(false);[m
[31m-            window.scrollTo({ top: 0, behavior: 'smooth' });[m
[31m-        }, 800);[m
[32m+[m[32m      setIsProcessing(true);[m
[32m+[m[32m      setTimeout(() => {[m
[32m+[m[32m        setActiveActionIndex(prev => prev + 1);[m
[32m+[m[32m        setIsProcessing(false);[m
[32m+[m[32m        window.scrollTo({ top: 0, behavior: 'smooth' });[m
[32m+[m[32m      }, 800);[m
     } else {[m
[31m-        const isPractice = tutorialData.mode === 'PRACTICE';[m
[31m-        const finalReport: Partial<Report> = {[m
[31m-            title: isPractice ? `PRACTICE: ${draftReport.category} REPORT` : `GENESIS: ${draftReport.category} REPORT`,[m
[31m-            description: draftReport.summary,[m
[31m-            category: (draftReport.category as Category) || Category.Other,[m
[31m-            location: tutorialData.operatingArea || 'Global Sector',[m
[31m-            severity: draftReport.severity,[m
[31m-            isActionable: draftReport.severity === 'Critical',[m
[31m-            trustScore: 100,[m
[31m-            imageUrls: evidence.items,[m
[31m-            isTutorial: true,[m
[31m-            structuredData: {[m
[31m-                tutorialMode: tutorialData.mode,[m
[31m-                riskAssessment: riskAssessment,[m
[31m-                fieldNotes: evidence.notes[m
[31m-            }[m
[31m-        };[m
[31m-        onComplete(finalReport, isPractice);[m
[32m+[m[32m      const isPractice = tutorialData.mode === 'PRACTICE';[m
[32m+[m[32m      const finalReport: Partial<Report> = {[m
[32m+[m[32m        title: isPractice ? `PRACTICE: ${draftReport.category} REPORT` : `GENESIS: ${draftReport.category} REPORT`,[m
[32m+[m[32m        description: draftReport.summary,[m
[32m+[m[32m        category: (draftReport.category as Category) || Category.Other,[m
[32m+[m[32m        location: tutorialData.operatingArea || 'Global Sector',[m
[32m+[m[32m        severity: draftReport.severity,[m
[32m+[m[32m        isActionable: draftReport.severity === 'Critical',[m
[32m+[m[32m        trustScore: 100,[m
[32m+[m[32m        imageUrls: evidence.items,[m
[32m+[m[32m        isTutorial: true,[m
[32m+[m[32m        structuredData: {[m
[32m+[m[32m          tutorialMode: tutorialData.mode,[m
[32m+[m[32m          riskAssessment: riskAssessment,[m
[32m+[m[32m          fieldNotes: evidence.notes[m
[32m+[m[32m        }[m
[32m+[m[32m      };[m
[32m+[m[32m      onComplete(finalReport, isPractice);[m
     }[m
   };[m
 [m
[36m@@ -106,12 +109,12 @@[m [mconst TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, o[m
     else if (entity === 'riskAssessment') value = (riskAssessment as any)[field];[m
 [m
     const dataTut = activeAction.id === 'tut_mode' ? 'mode' :[m
[31m-                    activeAction.id === 'tut_area' ? 'area' :[m
[31m-                    activeAction.id === 'tut_report_shell' ? 'category' :[m
[31m-                    activeAction.id === 'tut_event_context' ? 'summary' :[m
[31m-                    activeAction.id === 'tut_evidence' ? 'evidence' :[m
[31m-                    activeAction.id === 'tut_safety' ? 'safety' :[m
[31m-                    activeAction.id === 'tut_review' ? 'finalize' : '';[m
[32m+[m[32m      activeAction.id === 'tut_area' ? 'area' :[m
[32m+[m[32m        activeAction.id === 'tut_report_shell' ? 'category' :[m
[32m+[m[32m          activeAction.id === 'tut_event_context' ? 'summary' :[m
[32m+[m[32m            activeAction.id === 'tut_evidence' ? 'evidence' :[m
[32m+[m[32m              activeAction.id === 'tut_safety' ? 'safety' :[m
[32m+[m[32m                activeAction.id === 'tut_review' ? 'finalize' : '';[m
 [m
     const containerProps = dataTut ? { "data-tut": dataTut } : {};[m
 [m
[36m@@ -135,23 +138,23 @@[m [mconst TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, o[m
         );[m
       case 'checkbox':[m
         return ([m
[31m-            <button [m
[31m-                key={prompt.id}[m
[31m-                onClick={() => updateData(prompt, !value)}[m
[31m-                className={`w-full flex items-center space-x-4 p-5 rounded-2xl border-2 transition-all group ${value ? 'bg-emerald-950/20 border-emerald-500 text-white' : 'bg-zinc-950 border-zinc-800 text-zinc-500 hover:border-zinc-700'}`}[m
[31m-                {...containerProps}[m
[31m-            >[m
[31m-                <div className={`w-6 h-6 rounded-lg border-2 flex items-center justify-center transition-all ${value ? 'bg-emerald-500 border-emerald-400 shadow-[0_0_10px_rgba(16,185,129,0.5)]' : 'bg-black border-zinc-900 group-hover:border-zinc-700'}`}>[m
[31m-                    {value && <CheckCircle className="w-4 h-4 text-black" />}[m
[31m-                </div>[m
[31m-                <span className="text-[11px] font-black uppercase text-left">{prompt.promptText}</span>[m
[31m-            </button>[m
[32m+[m[32m          <button[m
[32m+[m[32m            key={prompt.id}[m
[32m+[m[32m            onClick={() => updateData(prompt, !value)}[m
[32m+[m[32m            className={`w-full flex items-center space-x-4 p-5 rounded-2xl border-2 transition-all group ${value ? 'bg-emerald-950/20 border-emerald-500 text-white' : 'bg-zinc-950 border-zinc-800 text-zinc-500 hover:border-zinc-700'}`}[m
[32m+[m[32m            {...containerProps}[m
[32m+[m[32m          >[m
[32m+[m[32m            <div className={`w-6 h-6 rounded-lg border-2 flex items-center justify-center transition-all ${value ? 'bg-emerald-500 border-emerald-400 shadow-[0_0_10px_rgba(16,185,129,0.5)]' : 'bg-black border-zinc-900 group-hover:border-zinc-700'}`}>[m
[32m+[m[32m              {value && <CheckCircle className="w-4 h-4 text-black" />}[m
[32m+[m[32m            </div>[m
[32m+[m[32m            <span className="text-[11px] font-black uppercase text-left">{prompt.promptText}</span>[m
[32m+[m[32m          </button>[m
         );[m
       case 'text':[m
         return ([m
           <div key={prompt.id} className="space-y-2" {...containerProps}>[m
             <label className="text-[10px] font-black text-cyan-500 uppercase tracking-widest">{prompt.promptText}</label>[m
[31m-            <textarea [m
[32m+[m[32m            <textarea[m
               value={value || ''}[m
               onChange={e => updateData(prompt, e.target.value)}[m
               className="w-full bg-zinc-950 border-2 border-zinc-800 rounded-2xl p-6 text-sm font-bold text-white outline-none focus:border-cyan-500 transition-all placeholder:text-zinc-900 min-h-[120px] shadow-inner"[m
[36m@@ -162,131 +165,131 @@[m [mconst TutorialMissionView: React.FC<TutorialMissionViewProps> = ({ onComplete, o[m
       case 'photo':[m
         return ([m
           <div key={prompt.id} className="space-y-4" {...containerProps}>[m
[31m-             <label className="text-[10px] font-black text-cyan-500 uppercase tracking-widest">{prompt.promptText}</label>[m
[31m-             <div className="aspect-video bg-zinc-950 border-2 border-dashed border-zinc-800 rounded-[2.5rem] flex flex-col items-center justify-center p-8 text-center cursor-pointer hover:border-cyan-500 transition-all group shadow-inner"[m
[31m-                  onClick={() => {[m
[31m-                      // Simulated upload[m
[31m-                      const dummy = "https://picsum.photos/seed/tutorial/800/600";[m
[31m-                      updateData(prompt, [dummy]);[m
[31m-                  }}>[m
[31m-                {value && value.length > 0 ? ([m
[31m-                    <img src={value[0]} alt="T" className="w-full h-full object-cover rounded-xl" />[m
[31m-                ) : ([m
[31m-                    <>[m
[31m-                        <Camera className="w-12 h-12 text-zinc-800 group-hover:text-cyan-400 transition-colors" />[m
[31m-                        <p className="mt-4 text-[10px] font-black uppercase text-zinc-700">Attach Sample Telemetry Shard</p>[m
[31m-                    </>[m
[31m-                )}[m
[31m-             </div>[m
[32m+[m[32m            <label className="text-[10px] font-black text-cyan-500 uppercase tracking-widest">{prompt.promptText}</label>[m
[32m+[m[32m            <div[m
[32m+[m[32m              className="aspect-video bg-zinc-950 border-2 border-dashed border-zinc-800 rounded-[2.5rem] flex flex-col items-center justify-center p-8 text-center cursor-pointer hover:border-cyan-500 transition-all group shadow-inner"[m
[32m+[m[32m              onClick={() => {[m
[32m+[m[32m                const dummy = "https://picsum.photos/seed/tutorial/800/600";[m
[32m+[m[32m                updateData(prompt, [dummy]);[m
[32m+[m[32m              }}[m
[32m+[m[32m            >[m
[32m+[m[32m              {value && value.length > 0 ? ([m
[32m+[m[32m                <img src={value[0]} alt="T" className="w-full h-full object-cover rounded-xl" />[m
[32m+[m[32m              ) : ([m
[32m+[m[32m                <>[m
[32m+[m[32m                  <Camera className="w-12 h-12 text-zinc-800 group-hover:text-cyan-400 transition-colors" />[m
[32m+[m[32m                  <p className="mt-4 text-[10px] font-black uppercase text-zinc-700">Attach Sample Telemetry Shard</p>[m
[32m+[m[32m                </>[m
[32m+[m[32m              )}[m
[32m+[m[32m            </div>[m
           </div>[m
[31m-        )[m
[31m-      default: return null;[m
[32m+[m[32m        );[m
[32m+[m[32m      default:[m
[32m+[m[32m        return null;[m
     }[m
   };[m
 [m
   return ([m
     <div className="bg-black min-h-screen text-white font-mono p-4 md:p-8 animate-fade-in">[m
[31m-        <TutorialOverlay [m
[31m-            step={overlayStep} [m
[31m-            onNext={canAdvance ? handleNext : () => alert("Protocol Warning: Action incomplete.")} [m
[31m-            onSkip={onSkip} [m
[31m-            isLastStep={activeActionIndex === tutorialReportForDutyMission.actions.length - 1} [m
[32m+[m[32m      {/* DO NOT SHOW ON LOAD. ONLY SHOW WHEN showTutorial IS TRUE. */}[m
[32m+[m[32m      {showTutorial && overlayStep && ([m
[32m+[m[32m        <TutorialOverlay[m
[32m+[m[32m          enabled={true}[m
[32m+[m[32m          step={overlayStep}[m
[32m+[m[32m          onNext={canAdvance ? handleNext : () => alert("Protocol Warning: Action incomplete.")}[m
[32m+[m[32m          onSkip={() => setShowTutorial(false)}[m
[32m+[m[32m          isLastStep={activeActionIndex === tutorialReportForDutyMission.actions.length - 1}[m
         />[m
[32m+[m[32m      )}[m
 [m
[31m-        <div className="max-w-6xl mx-auto space-y-12 pb-32">[m
[31m-            <header className="flex justify-between items-center bg-zinc-900/50 p-8 rounded-[3rem] border border-zinc-800 shadow-2xl relative overflow-hidden">[m
[31m-                <div className="absolute top-0 right-0 p-10 opacity-5 pointer-events-none"><Target className="w-48 h-48 text-cyan-500"/></div>[m
[31m-                <div className="flex items-center space-x-8">[m
[31m-                    <div className="p-4 bg-cyan-600 rounded-2xl shadow-3xl border-b-4 border-cyan-800 animate-pulse">[m
[31m-                        <ShieldCheck className="w-10 h-10 text-white" />[m
[31m-                    </div>[m
[31m-                    <div>[m
[31m-                        <h1 className="text-3xl font-black uppercase tracking-tighter leading-none">Protocol_Genesis</h1>[m
[31m-                        <p className="text-[10px] font-bold text-zinc-500 uppercase tracking-[0.4em] mt-2">Guided Induction Mission v1.0</p>[m
[32m+[m[32m      <div className="max-w-6xl mx-auto space-y-12 pb-32">[m
[32m+[m[32m        <header className="flex justify-between items-center bg-zinc-900/50 p-8 rounded-[3rem] border border-zinc-800 shadow-2xl relative overflow-hidden">[m
[32m+[m[32m          <div className="absolute top-0 right-0 p-10 opacity-5 pointer-events-none"><Target className="w-48 h-48 text-cyan-500" /></div>[m
[32m+[m[32m          <div className="flex items-center space-x-8">[m
[32m+[m[32m            <div className="p-4 bg-cyan-600 rounded-2xl shadow-3xl border-b-4 border-cyan-800 animate-pulse">[m
[32m+[m[32m              <ShieldCheck className="w-10 h-10 text-white" />[m
[32m+[m[32m            </div>[m
[32m+[m[32m            <div>[m
[32m+[m[32m              <h1 className="text-3xl font-black uppercase tracking-tighter leading-none">Protocol_Genesis</h1>[m
[32m+[m[32m              <p className="text-[10px] font-bold text-zinc-500 uppercase tracking-[0.4em] mt-2">Guided Induction Mission v1.0</p>[m
[32m+[m[32m            </div>[m
[32m+[m[32m          </div>[m
[32m+[m
[32m+[m[32m          {/* OPTIONAL: Button to manually open tutorial */}[m
[32m+[m[32m          <div className="hidden md:flex items-center space-x-3">[m
[32m+[m[32m            <button[m
[32m+[m[32m              onClick={() => setShowTutorial(true)}[m
[32m+[m[32m              className="bg-zinc-900 hover:bg-zinc-800 text-zinc-300 px-6 py-3 rounded-xl border border-zinc-800 text-[10px] font-black uppercase tracking-widest transition-all"[m
[32m+[m[32m            >[m
[32m+[m[32m              Help_Tutorial[m
[32m+[m[32m            </button>[m
[32m+[m[32m          </div>[m
[32m+[m[32m        </header>[m
[32m+[m
[32m+[m[32m        <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">[m
[32m+[m[32m          {/* Timeline Rail */}[m
[32m+[m[32m          <aside className="lg:col-span-3 space-y-4">[m
[32m+[m[32m            <div className="bg-zinc-900 border border-zinc-800 p-8 rounded-[2.5rem] space-y-10 relative shadow-inner overflow-hidden">[m
[32m+[m[32m              <div className="absolute left-[47px] top-12 bottom-12 w-0.5 bg-zinc-800"></div>[m
[32m+[m[32m              {tutorialReportForDutyMission.actions.map((act, idx) => {[m
[32m+[m[32m                const isCurrent = idx === activeActionIndex;[m
[32m+[m[32m                const isPast = idx < activeActionIndex;[m
[32m+[m[32m                return ([m
[32m+[m[32m                  <div key={act.id} className={`flex items-center space-x-6 relative z-10 transition-all duration-500 ${idx > activeActionIndex ? 'opacity-20 grayscale' : 'opacity-100'}`}>[m
[32m+[m[32m                    <div className={`w-10 h-10 rounded-xl border-2 flex items-center justify-center transition-all ${isPast ? 'bg-emerald-500 border-emerald-400 text-black shadow-[0_0_10px_rgba(16,185,129,0.5)]' : isCurrent ? 'bg-cyan-500 border-cyan-400 text-white shadow-[0_0_15px_cyan]' : 'bg-zinc-950 border-zinc-800 text-zinc-700'}`}>[m
[32m+[m[32m                      {isPast ? <CheckCircle className="w-5 h-5" /> : act.id.startsWith('tut_evidence') ? <Camera className="w-4 h-4" /> : act.id.startsWith('tut_safety') ? <ShieldCheck className="w-4 h-4" /> : <Activity className="w-4 h-4" />}[m
                     </div>[m
[31m-                </div>[m
[31m-                <div className="text-right hidden md:block">[m
[31m-                     <p className="text-[9px] font-black text-zinc-500 uppercase tracking-widest mb-1">Mission_Progress</p>[m
[31m-                     <div className="flex items-center space-x-3">[m
[31m-                         <span className="text-2xl font-black text-cyan-400">{Math.round(((activeActionIndex + 1) / tutorialReportForDutyMission.actions.length) * 100)}%</span>[m
[31m-                         <div className="w-32 h-2 bg-zinc-800 rounded-full overflow-hidden">[m
[31m-                             <div className="h-full bg-cyan-500 shadow-[0_0_10px_cyan]" style={{ width: `${((activeActionIndex + 1) / tutorialReportForDutyMission.actions.length) * 100}%` }}></div>[m
[31m-                         </div>[m
[31m-                     </div>[m
[31m-                </div>[m
[31m-            </header>[m
[32m+[m[32m                    <span className={`text-[9px] font-black uppercase tracking-widest ${isCurrent ? 'text-white' : 'text-zinc-500'}`}>{act.title.replace(' ', '_')}</span>[m
[32m+[m[32m                  </div>[m
[32m+[m[32m                );[m
[32m+[m[32m              })}[m
[32m+[m[32m            </div>[m
[32m+[m[32m          </aside>[m
 [m
[31m-            <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">[m
[31m-                {/* Timeline Rail */}[m
[31m-                <aside className="lg:col-span-3 space-y-4">[m
[31m-                    <div className="bg-zinc-900 border border-zinc-800 p-8 rounded-[2.5rem] space-y-10 relative shadow-inner overflow-hidden">[m
[31m-                        <div className="absolute left-[47px] top-12 bottom-12 w-0.5 bg-zinc-800"></div>[m
[31m-                        {tutorialReportForDutyMission.actions.map((act, idx) => {[m
[31m-                            const isCurrent = idx === activeActionIndex;[m
[31m-                            const isPast = idx < activeActionIndex;[m
[31m-                            return ([m
[31m-                                <div key={act.id} className={`flex items-center space-x-6 relative z-10 transition-all duration-500 ${idx > activeActionIndex ? 'opacity-20 grayscale' : 'opacity-100'}`}>[m
[31m-                                    <div className={`w-10 h-10 rounded-xl border-2 flex items-center justify-center transition-all ${[m
[31m-                                        isPast ? 'bg-emerald-500 border-emerald-400 text-black shadow-[0_0_10px_rgba(16,185,129,0.5)]' :[m
[31m-                                        isCurrent ? 'bg-cyan-500 border-cyan-400 text-white shadow-[0_0_15px_cyan]' :[m
[31m-                                        'bg-zinc-950 border-zinc-800 text-zinc-700'[m
[31m-                                    }`}>[m
[31m-                                        {isPast ? <CheckCircle className="w-5 h-5" /> : act.id.startsWith('tut_evidence') ? <Camera className="w-4 h-4"/> : act.id.startsWith('tut_safety') ? <ShieldCheck className="w-4 h-4"/> : <Activity className="w-4 h-4"/>}[m
[31m-                                    </div>[m
[31m-                                    <span className={`text-[9px] font-black uppercase tracking-widest ${isCurrent ? 'text-white' : 'text-zinc-500'}`}>{act.title.replace(' ', '_')}</span>[m
[31m-                                </div>[m
[31m-                            );[m
[31m-                        })}[m
[32m+[m[32m          {/* Main Action Console */}[m
[32m+[m[32m          <main className="lg:col-span-9">[m
[32m+[m[32m            <PanelShell id="action_console" title="Directive_Execution_Link">[m
[32m+[m[32m              <div className="p-8 md:p-12 space-y-12">[m
[32m+[m[32m                <div className="space-y-4">[m
[32m+[m[32m                  <div className="flex justify-between items-start">[m
[32m+[m[32m                    <div className="flex items-center space-x-4">[m
[32m+[m[32m                      <div className="p-3 bg-cyan-950/40 rounded-xl border border-cyan-900/40">[m
[32m+[m[32m                        <Zap className="w-6 h-6 text-cyan-400" />[m
[32m+[m[32m                      </div>[m
[32m+[m[32m                      <h2 className="text-4xl font-black uppercase tracking-tighter text-white">{activeAction.title}</h2>[m
                     </div>[m
[31m-                </aside>[m
[32m+[m[32m                    <button onClick={onSkip} className="bg-zinc-900 hover:bg-rose-950 text-zinc-500 hover:text-rose-500 px-6 py-3 rounded-xl border border-zinc-800 text-[10px] font-black uppercase tracking-widest transition-all">[m
[32m+[m[32m                      Exit_Induction[m
[32m+[m[32m                    </button>[m
[32m+[m[32m                  </div>[m
[32m+[m[32m                  <p className="text-base text-zinc-400 font-bold leading-relaxed border-l-4 border-cyan-500 pl-8 italic uppercase">[m
[32m+[m[32m                    "{activeAction.objective}"[m
[32m+[m[32m                  </p>[m
[32m+[m[32m                </div>[m
 [m
[31m-                {/* Main Action Console */}[m
[31m-                <main className="lg:col-span-9">[m
[31m-                    <PanelShell id="action_console" title="Directive_Execution_Link">[m
[31m-                         <div className="p-8 md:p-12 space-y-12">[m
[31m-                            <div className="space-y-4">[m
[31m-                                <div className="flex justify-between items-start">[m
[31m-                                    <div className="flex items-center space-x-4">[m
[31m-                                        <div className="p-3 bg-cyan-950/40 rounded-xl border border-cyan-900/40">[m
[31m-                                            <Zap className="w-6 h-6 text-cyan-400" />[m
[31m-                                        </div>[m
[31m-                                        <h2 className="text-4xl font-black uppercase tracking-tighter text-white">{activeAction.title}</h2>[m
[31m-                                    </div>[m
[31m-                                    <button onClick={onSkip} className="bg-zinc-900 hover:bg-rose-950 text-zinc-500 hover:text-rose-500 px-6 py-3 rounded-xl border border-zinc-800 text-[10px] font-black uppercase tracking-widest transition-all">[m
[31m-                                        Exit_Induction[m
[31m-                                    </button>[m
[31m-                                </div>[m
[31m-                                <p className="text-base text-zinc-400 font-bold leading-relaxed border-l-4 border-cyan-500 pl-8 italic uppercase">[m
[31m-                                    "{activeAction.objective}"[m
[31m-                                </p>[m
[31m-                            </div>[m
[32m+[m[32m                <div className="space-y-10">[m
[32m+[m[32m                  {activeAction.requiredPrompts.map(prompt => renderPrompt(prompt))}[m
[32m+[m[32m                </div>[m
 [m
[31m-                            <div className="space-y-10">[m
[31m-                                {activeAction.requiredPrompts.map(prompt => renderPrompt(prompt))}[m
[31m-                            </div>[m
[32m+[m[32m                <div className="pt-10 border-t border-zinc-800">[m
[32m+[m[32m                  <button[m
[32m+[m[32m                    onClick={handleNext}[m
[32m+[m[32m                    disabled={!canAdvance || isProcessing}[m
[32m+[m[32m                    className={`w-full py-8 rounded-[2rem] font-black uppercase tracking-[0.5em] text-xs transition-all shadow-4xl active:scale-[0.98] flex items-center justify-center space-x-6 border-b-8 ${canAdvance ? 'bg-white text-black border-zinc-300 hover:bg-cyan-50' : 'bg-zinc-900 text-zinc-700 border-zinc-950 opacity-40 cursor-not-allowed'}`}[m
[32m+[m[32m                  >[m
[32m+[m[32m                    {isProcessing ? <Loader className="w-6 h-6 animate-spin" /> : <Sparkles className="w-6 h-6 text-cyan-500" />}[m
[32m+[m[32m                    <span>{activeActionIndex === tutorialReportForDutyMission.actions.length - 1 ? 'SEAL_AND_INITIALIZE_ACCOUNT' : 'SYNCHRONIZE_PHASE'}</span>[m
[32m+[m[32m                  </button>[m
 [m
[31m-                            <div className="pt-10 border-t border-zinc-800">[m
[31m-                                <button [m
[31m-                                    onClick={handleNext}[m
[31m-                                    disabled={!canAdvance || isProcessing}[m
[31m-                                    className={`w-full py-8 rounded-[2rem] font-black uppercase tracking-[0.5em] text-xs transition-all shadow-4xl active:scale-[0.98] flex items-center justify-center space-x-6 border-b-8 ${[m
[31m-                                        canAdvance [m
[31m-                                        ? 'bg-white text-black border-zinc-300 hover:bg-cyan-50' [m
[31m-                                        : 'bg-zinc-900 text-zinc-700 border-zinc-950 opacity-40 cursor-not-allowed'[m
[31m-                                    }`}[m
[31m-                                >[m
[31m-                                    {isProcessing ? <Loader className="w-6 h-6 animate-spin"/> : <Sparkles className="w-6 h-6 text-cyan-500"/>}[m
[31m-                                    <span>{activeActionIndex === tutorialReportForDutyMission.actions.length - 1 ? 'SEAL_AND_INITIALIZE_ACCOUNT' : 'SYNCHRONIZE_PHASE'}</span>[m
[31m-                 